<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Edit Data Pesanan</title>
    <link rel="stylesheet" href="/style.css"> 
</head>
<body>
    <div class="edit-user-section">
        <h2>Edit Data Pesanan</h2>
        <form action="/update/<%= order.id %>" method="POST"> 
            <input type="hidden" name="id" value="<%= order.id %>"> 
            <div class="form-group">
                <label for="username">Nama Pengguna:</label>
                <input type="text" id="username" name="username" required value="<%= order.username %>">
            </div>
            <div class="form-group">
                <label for="table_number">Nomor Meja:</label>
                <input type="number" name="table_number" id="table_number" required value="<%= order.table_number %>">
            </div>
            <div class="form-group">
                <label for="order">Pesanan:</label>
                <input type="text" name="order" id="order" required value="<%= order.order %>">
            </div>
            <button type="submit" style="float: right;">Simpan</button> 
        </form>
    </div>
</body>
</html>

